# Tests for fetching nodes

defmodule KnowledgeGraph.KnowledgeGraphTest do
  use ExUnit.Case

  # test "list_nodes/0 returns a list of nodes" do
  #   assert KnowledgeGraph.KnowledgeGraph.list_nodes() |> Enum.count() > 0
  # end
end
